services:
  # Backend Service (Docker)
  - type: web
    name: agentic-healthcare-sys
    env: docker
    plan: free
    dockerContext: .
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MODEL_NAME
        value: gpt-4o-mini
